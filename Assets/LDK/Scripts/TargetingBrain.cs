using UnityEngine;

public class TargetingBrain : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    private LineupSlot GetFrontGuardSlot(bool isEnemy)
    {
        if (isEnemy)
        {


        }
        else
        {

        }

        return null;
    }

}
